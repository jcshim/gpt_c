## 9장. 포인터 — 메모리를 직접 가리키는 힘

---

### 1. 포인터를 왜 배울까?

지금까지 우리는 이렇게 써 왔다.

```c
int a = 10;
```

이때 컴퓨터 메모리 어딘가에 `10`이 저장된다.
**포인터(pointer)** 는 바로 이 질문에서 시작한다.

👉 “그 값은 **어디에** 저장되어 있을까?”

---

### 2. 주소(address)란?

```c
int a = 10;
printf("%p\n", &a);
```

* `&a` : 변수 `a`의 **메모리 주소**
* `%p` : 주소 출력 형식

👉 변수는 **값 + 주소**를 가진다.

---

### 3. 포인터 변수 선언

```c
int *p;
```

* `p` : 포인터 변수
* `int *` : int형 값을 가리키는 포인터

---

### 4. 포인터에 주소 저장

```c
int a = 10;
int *p = &a;
```

관계 정리:

```
a   → 값 10
p   → a의 주소
```

---

### 5. 포인터로 값 접근하기 (역참조)

```c
printf("%d\n", *p);
```

* `*p` : p가 가리키는 **값**
* 결과: `10`

👉
`p`는 주소
`*p`는 값

---

### 6. 값 변경도 가능하다

```c
*p = 20;
printf("%d\n", a);
```

출력:

```
20
```

👉 포인터를 통해 **원래 변수 값이 바뀐다**

---

### 7. 그림으로 이해하기

```
변수 a     : 10
주소 &a    : 0x1000 (예시)
포인터 p  : 0x1000
*p        : 10
```

---

### 8. 함수에서 값이 안 바뀌는 이유

```c
void change(int x)
{
    x = 100;
}

int main(void)
{
    int a = 10;
    change(a);
    printf("%d\n", a); // 10
}
```

👉 함수는 **값 복사(Call by Value)**

---

### 9. 포인터로 값 바꾸기

```c
void change(int *p)
{
    *p = 100;
}

int main(void)
{
    int a = 10;
    change(&a);
    printf("%d\n", a); // 100
}
```

👉 **Call by Address**

---

### 10. swap 함수 완성하기

```c
void swap(int *a, int *b)
{
    int t = *a;
    *a = *b;
    *b = t;
}
```

사용:

```c
int x = 3, y = 7;
swap(&x, &y);
```

---

### 11. 배열과 포인터의 관계

```c
int a[3] = {10, 20, 30};
int *p = a;
```

* `a` == `&a[0]`
* `p[1]` == `a[1]`

---

### 12. 포인터 연산

```c
printf("%d\n", *(p + 1));
```

* 다음 요소로 이동
* 자료형 크기만큼 자동 이동

---

### 13. 문자열과 포인터

```c
char *s = "hello";
printf("%c\n", *s);      // h
printf("%c\n", *(s+1));  // e
```

👉 문자열은 **char 배열 + 포인터**

---

### 14. 이중 포인터 맛보기

```c
int a = 10;
int *p = &a;
int **pp = &p;
```

* `pp` → `p`의 주소
* `**pp` → `a`의 값

👉 나중에 **동적 메모리, 리스트**에서 필수

---

### 15. 포인터에서 가장 많이 나는 에러

❌ 초기화 안 된 포인터 사용
❌ NULL 포인터 접근
❌ 배열 범위 초과 접근

```c
int *p;   // 위험!
*p = 10;  // ❌
```

---

### 16. 안전한 습관

```c
int *p = NULL;
```

👉 포인터는 항상 **초기화**

---

### 17. GPT에게 이렇게 질문하자

> 💬 “포인터를 그림으로 설명해 줘”
> 💬 “swap 함수 원리 알려줘”
> 💬 “배열과 포인터 차이 예제로 보여줘”

---

### 18. 지금 단계에서 할 수 있어야 할 것

* 주소(`&`)와 포인터(`*`) 구분
* 함수에서 값 변경
* 배열 ↔ 포인터 관계 이해
* swap 함수 직접 작성

---

다음 장에서는
**동적 메모리(malloc/free)** —
진짜 C 프로그래밍의 시작을 배운다.
